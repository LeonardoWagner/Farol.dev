// Honest placeholder — no fabricated testimonials. Populate as real client
// feedback comes in; the Testimonials section renders an honest empty state
// while this array is empty.
export type Testimonial = {
  quote: string;
  author: string;
  role: string;
};

export const testimonials: Testimonial[] = [];
